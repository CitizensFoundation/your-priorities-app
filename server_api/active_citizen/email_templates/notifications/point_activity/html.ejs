<% include ../header.html.ejs %>

<% if (activity.type == 'activity.point.new') { %>
  <p><%= post.id == user.id ? t('notification.email.point.new') : t('notification.email.point.new_self') %></p>
<% } %>
<% else if (activity.type == 'activity.point.unhelpful') { %>
  <p><%= post.id == user.id ? t('notification.email.point.unhelpful') : t('notification.email.point.unhelpful_self') %></p>
<% } %>
<% else if (activity.type == 'activity.point.helpful') { %>
  <p><%= post.id == user.id ? t('notification.email.point.helpful') : t('notification.email.point.helpful_self') %></p>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+point.Post.id+"/"+point.id %>

<% include ../footer.html.ejs %>
