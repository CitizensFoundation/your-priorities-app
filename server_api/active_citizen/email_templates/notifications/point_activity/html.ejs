<% include ../header.html.ejs %>

<% if (activity.type == 'activity.point.new') { %>
  <p><%= post.id == user.id ? t('notification.email.new_point') : t('notification.email.new_point_self') %></p>
<% } %>
<% else if (activity.type == 'activity.point.unhelpful') { %>
  <p><%= post.id == user.id ? t('notification.email.unhelpful') : t('notification.email.unhelpful_self') %></p>
<% } %>
<% else if (activity.type == 'activity.point.helpful') { %>
  <p><%= post.id == user.id ? t('notification.email.helpful') : t('notification.email.helpful_self') %></p>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+point.Post.id+"/"+point.id %>

<% include ../footer.html.ejs %>
