<% include ../header.text.ejs %>

<% if (activity.type == 'activity.point.new') { %>
<%= post.id == user.id ? t('notification.email.new_point_1') : t('notification.email.new_point_1_self') %>
<% } %>
<% else if (activity.type == 'activity.point.unhelpful') { %>
<%= post.id == user.id ? t('notification.email.unhelpful') : t('notification.email.unhelpful_self') %>
<% } %>
<% else if (activity.type == 'activity.point.helpful') { %>
<%= post.id == user.id ? t('notification.email.helpful_1') : t('notification.email.helpful_1_self') %>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+post.id %>

<% include ../footer.text.ejs %>