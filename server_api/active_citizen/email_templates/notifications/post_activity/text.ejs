<% include ../header.html.ejs %>

<% if (activity.type == 'activity.post.new') { %>
  <%= post.id == user.id ? t('notification.email.new_point') : t('notification.email.new_point_self') %>
<% } %>
<% else if (activity.type == 'activity.endorsement.new') { %>
  <%= post.id == user.id ? t('notification.email.endorsement') : t('notification.email.endorsement_self') %>
<% } %>
<% else if (activity.type == 'activity.opposition.new') { %>
  <%= post.id == user.id ? t('notification.email.opposition') : t('notification.email.opposition_self') %>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+post.id %>

<% include ../footer.html.ejs %>
