<% include ../header.html.ejs %>

<% if(activity.type == 'activity.post.new') { %>
<p><%= post.id == user.id ? t('notification.email.new_post_1') : t('notification.email.new_post_1')%></p>
<% } %>


<% if (activity.type == 'activity.post.new') { %>
  <p><%= post.id == user.id ? t('notification.email.new_point') : t('notification.email.new_point_self') %></p>
<% } %>
<% else if (activity.type == 'activity.endorsement.new') { %>
  <p><%= post.id == user.id ? t('notification.email.endorsement') : t('notification.email.endorsement_self') %></p>
<% } %>
<% else if (activity.type == 'activity.opposition.new') { %>
  <p><%= post.id == user.id ? t('notification.email.opposition') : t('notification.email.opposition_self') %></p>
<% } %>


<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+post.id %>

<% include ../footer.html.ejs %>
